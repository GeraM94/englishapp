{
  "name": "englishapp",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:server": "npm run dev --workspace englishapp-server",
    "dev:client": "npm run dev --workspace englishapp-client",
    "build": "npm run build --workspace englishapp-client",
    "start": "npm run start --workspace englishapp-server",
    "lint": "npm run lint --workspace englishapp-server && npm run lint --workspace englishapp-client",
    "test": "npm run test --workspace englishapp-server && npm run test --workspace englishapp-client"
  }
}